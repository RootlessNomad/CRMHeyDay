# Frontend env (NEXT_PUBLIC_* son las únicas expuestas al navegador).
# Copia este archivo a `frontend/.env.local` y ajusta si tu backend corre en otro puerto.
NEXT_PUBLIC_API_URL=http://localhost:3001
