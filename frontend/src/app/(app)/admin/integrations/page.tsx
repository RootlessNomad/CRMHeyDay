import { ComingSoonPage } from '@/components/ComingSoonPage';

export default function AdminIntegrationsPage(): JSX.Element {
  return (
    <ComingSoonPage
      title="Integraciones"
      description="Estado de salud de las integraciones activas con servicios externos."
      milestone="M3 — Admin Panel"
    />
  );
}
