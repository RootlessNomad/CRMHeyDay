import { ComingSoonPage } from '@/components/ComingSoonPage';

export default function AdminAiCostsPage(): JSX.Element {
  return (
    <ComingSoonPage
      title="Costes IA"
      description="Monitoriza el consumo y coste de las llamadas a Claude por feature y usuario."
      milestone="M3 — Admin Panel"
    />
  );
}
