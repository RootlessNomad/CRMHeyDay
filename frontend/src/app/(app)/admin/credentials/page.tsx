import { ComingSoonPage } from '@/components/ComingSoonPage';

export default function AdminCredentialsPage(): JSX.Element {
  return (
    <ComingSoonPage
      title="Credenciales y API keys"
      description="Gestiona las integraciones de terceros: añade, rota y prueba API keys de forma segura."
      milestone="M3 — Admin Panel"
    />
  );
}
