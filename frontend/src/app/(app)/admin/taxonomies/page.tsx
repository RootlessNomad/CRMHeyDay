import { ComingSoonPage } from '@/components/ComingSoonPage';

export default function AdminTaxonomiesPage(): JSX.Element {
  return (
    <ComingSoonPage
      title="Taxonomías"
      description="Edita los catálogos de pain points, líneas de servicio y pilares de contenido sin necesidad de deploy."
      milestone="M3 — Admin Panel"
    />
  );
}
